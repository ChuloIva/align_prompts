# Core dependencies
openai>=1.0.0
pandas>=2.0.0
matplotlib>=3.7.0
seaborn>=0.12.0
pydantic>=2.0.0
pyyaml>=6.0

# Graph processing (for SGM)
networkx>=3.0
tqdm>=4.65.0

# Visualization (for SGM)
plotly>=5.0.0
pyvis>=0.3.0
umap-learn>=0.5.0
scikit-learn>=1.0.0
scipy>=1.10.0

# Optional dependencies
# vLLM (for local inference with GPU)
vllm>=0.2.0

# Jupyter notebook support
jupyter>=1.0.0
ipykernel>=6.0.0
ipywidgets>=8.0.0

# Development dependencies (optional)
pytest>=7.0.0
black>=23.0.0
mypy>=1.0.0
